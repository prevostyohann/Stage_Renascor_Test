{% extends 'base.html.twig' %}

{% block title %}Menu Client{% endblock %}

{% block body %}
<div class="flex flex-col items-center p-8 font-sans">
    <img src="{{ asset('uploads/BloomCare.png') }}" alt="Logo" class="mx-auto h-10">
    <div class="font-bold text-2xl mb-8">Menu</div>

<div class="flex flex-col space-y-4">
    <a href="{{ path('app_office_list') }}" class="flex items-center text-lg text-purple-900 hover:translate-x-1 transition-transform">
        <span class="text-purple-700 text-xl mr-4">🔍</span> Rechercher
    </a>

    <a href="{{ path('client_appointments') }}" class="flex items-center text-lg text-purple-900 hover:translate-x-1 transition-transform">
        <span class="text-purple-700 text-xl mr-4">✅</span> Mes Réservations
    </a>

    <a href="{{ path('client_reviews') }}" class="flex items-center text-lg text-purple-900 hover:translate-x-1 transition-transform">
        <span class="text-purple-700 text-xl mr-4">⭐</span> Avis
    </a>

    <a href="{{ path('profil_show') }}" class="flex items-center text-lg text-purple-900 hover:translate-x-1 transition-transform">
        <span class="text-purple-700 text-xl mr-4">👤</span> Compte
    </a>

    <a href="{{ path('app_about') }}" class="flex items-center text-lg text-purple-900 hover:translate-x-1 transition-transform">
        <span class="text-purple-700 text-xl mr-4">❤️</span> A Propos
    </a>

    <a href="{{ path('app_settings') }}" class="flex items-center text-lg text-purple-900 hover:translate-x-1 transition-transform">
        <span class="text-purple-700 text-xl mr-4">⚙️</span> Paramètres
    </a>
</div>

        
</div>
{% endblock %}
