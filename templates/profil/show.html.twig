{% extends 'base.html.twig' %}

{% block title %}Mon Profil{% endblock %}

{% block body %}
<div class="p-8 max-w-3xl mx-auto font-sans bg-gray-50 min-h-screen">

    <div class="sticky top-0 z-50 bg-white flex items-center justify-between mt-0 py-4 border-b border-gray-200">
        {% set back_route = is_granted('ROLE_CLIENT') ? 'client_home' : (is_granted('ROLE_PRO') ? 'pro_home' : null) %}

        {% if back_route %}
            <a href="{{ path(back_route) }}" class="flex items-center text-blue-700 font-semibold hover:underline">
                <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7" />
                </svg>
                Retour
            </a>
            <img src="{{ asset('uploads/BloomCare.png') }}" alt="Logo" class="mx-auto h-10">
        {% endif %}
    </div>

    <div class="bg-white border border-purple-700 rounded-xl shadow-md mt-10 p-6">
        <h1 class="text-2xl font-bold text-purple-700 mb-6 text-center">👤 Mon profil</h1>

        <ul class="space-y-3 text-gray-700">
            <li><span class="font-semibold">Email :</span> {{ user.email }}</li>
            <li><span class="font-semibold">Nom d'utilisateur :</span> {{ user.username }}</li>
            <li><span class="font-semibold">Nom :</span> {{ user.lastName }}</li>
            <li><span class="font-semibold">Prénom :</span> {{ user.firstName }}</li>
            <li><span class="font-semibold">Adresse :</span> {{ user.addressComplement }} {{ user.address }}</li>
            <li><span class="font-semibold">Code postal :</span> {{ user.postalCode }}</li>
            <li><span class="font-semibold">Ville :</span> {{ user.city }}</li>
            <li><span class="font-semibold">Téléphone :</span> {{ user.phoneNumber }}</li>
            <li><span class="font-semibold">Date de naissance :</span> {{ user.birthDate ? user.birthDate|date('d/m/Y') : 'N/A' }}</li>
        </ul>

        <div class="text-center mt-8">
            <a href="{{ path('profil_edit') }}" class="inline-block bg-purple-600 hover:bg-purple-700 text-white font-medium py-2 px-5 rounded-xl transition">
                Modifier mon profil
            </a>
        </div>
    </div>
</div>
{% endblock %}
